return {
  "akinsho/bufferline.nvim",
  enabled = false,
}
-- return {
--   "akinsho/bufferline.nvim",
--   -- enabled = false,
--   opts = {
--     options = {
--       -- mode = "tabs",
--       numbers = function(opts) -- Adds the buffer number to the tabline
--         -- return string.format("%s.%s", opts.lower(opts.id), opts.lower(opts.ordinal))
--         return string.format("%s", opts.ordinal)
--       end,
--     },
--   },
-- }
